#include<bits/stdc++.h>

using namespace std;

int main() {
    set<int> deleted;
    priority_queue<int> pc;
    queue<int> mc;

    int q;
    cin >> q;
    string ans;
    while(q--) {
        int a;
        cin >> a;
        if(a == 1) {
            int m;
            cin >> m;
            pc.push_back(pair<int, int>(m, num));
            mc.push_back(num);
            num++;
        } else if(a == 2) {
        } else {
        }
    }
}